# Read-only MongoDB Atlas user. Used only during the Vercel build; never sent
# to browser JavaScript.
MONGO_URI=mongodb+srv://dashboard_reader:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority

# Optional overrides
MONGO_DB_NAME=elderly_analysis
MONGO_COLLECTION_NAME=elderly_data
